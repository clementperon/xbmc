diff -Naur a/meson.build b/meson.build
--- a/meson.build	2026-07-13 16:07:35
+++ b/meson.build	2026-08-11 13:29:40
@@ -67,7 +67,9 @@
 if host_machine.system() == 'windows'\
    and cc.has_header('winioctl.h', args: test_args)
    # Windows is supported as long as winioctl.h is found
-elif host_machine.system() == 'darwin'
+elif host_machine.system() == 'darwin' \
+     and cc.has_header('IOKit/storage/IODVDMediaBSDClient.h', args: test_args)
+    # iOS and tvOS also report as darwin but ship no DVD IOKit headers
     add_project_arguments(cc.get_supported_arguments('-no-cpp-precomp'), language: 'c')
     cdata.set('DARWIN_DVD_IOCTL', 1)
     extra_dependencies += dependency('appleframeworks', modules: ['CoreFoundation', 'IOKit'])
@@ -96,7 +98,8 @@
     cdata.set('DVD_STRUCT_IN_SYS_DVDIO_H', 1)
     cdata.set('HAVE_BSD_DVD_STRUCT', 1)
 else
-    warning('No DVD ioctls found for this system. libdvdcss will probably fail to build.')
+    cdata.set('DVDCSS_NO_IOCTLS', 1)
+    warning('No DVD ioctls found for this system. Title keys will be cracked rather than read from the drive.')
 endif
 
 posix_mkdir = ''
diff -Naur a/src/device.c b/src/device.c
--- a/src/device.c	2026-08-11 13:29:40
+++ b/src/device.c	2026-08-11 13:29:40
@@ -106,7 +106,9 @@
     if( dvdcss->p_stream )
         return 0;
 
-#if defined( _WIN32 )
+#if defined( DVDCSS_NO_IOCTLS )
+    return 0;
+#elif defined( _WIN32 )
     if( dvdcss->b_file )
     {
         return 0;
diff -Naur a/src/ioctl.c b/src/ioctl.c
--- a/src/ioctl.c	2026-07-13 16:07:35
+++ b/src/ioctl.c	2026-08-11 13:29:40
@@ -231,7 +231,11 @@
     *pi_copyright = p_buffer[ 4 ];
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)i_layer;
+    (void)pi_copyright;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -361,7 +365,11 @@
     memcpy( p_key, p_buffer + 4, DVD_DISCKEY_SIZE );
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)pi_agid;
+    (void)p_key;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -493,7 +501,12 @@
     memcpy( p_key, p_buffer + 5, DVD_KEY_SIZE );
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)pi_agid;
+    (void)i_pos;
+    (void)p_key;
+    i_ret = -1;
 
 #endif
 
@@ -592,7 +605,10 @@
     *pi_agid = p_buffer[ 7 ] >> 6;
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)pi_agid;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -702,7 +718,11 @@
     memcpy( p_challenge, p_buffer + 4, DVD_CHALLENGE_SIZE );
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)pi_agid;
+    (void)p_challenge;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -814,7 +834,10 @@
     *pi_asf = p_buffer[ 7 ] & 1;
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)pi_asf;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -919,7 +942,11 @@
     memcpy( p_key, p_buffer + 4, DVD_KEY_SIZE );
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)pi_agid;
+    (void)p_key;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -1004,7 +1031,10 @@
                         &sdc, sizeof(sdc), &ulParamLen,
                         NULL, 0, &ulDataLen);
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)pi_agid;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -1123,7 +1153,11 @@
         memcpy(p_data_buffer, p_buffer, CPRM_MEDIA_ID_SIZE);
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)p_agid;
+    (void)p_data_buffer;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -1272,7 +1306,13 @@
     }
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)p_agid;
+    (void)mkb_pack;
+    (void)p_mkb_pack;
+    (void)p_total_packs;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -1384,7 +1424,11 @@
                          p_buffer, sizeof(p_buffer), &ulDataLen );
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)pi_agid;
+    (void)p_challenge;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -1495,7 +1539,11 @@
                          p_buffer, sizeof(p_buffer), &ulDataLen );
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)pi_agid;
+    (void)p_key;
+    i_ret = -1;
 
 #endif
     return i_ret;
@@ -1624,7 +1672,12 @@
     *p_scheme = p_buffer[ 6 ];
 
 #else
-#   error "DVD ioctls are unavailable on this system"
+    /* No DVD ioctls on this platform. */
+    (void)i_fd;
+    (void)p_type;
+    (void)p_mask;
+    (void)p_scheme;
+    i_ret = -1;
 
 #endif
     return i_ret;
